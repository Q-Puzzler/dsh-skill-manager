# Peer packages of the dsh type-only devDependencies are not installed: the
# runtime satisfies them through the module graph, and skipLibCheck covers the
# declaration-file gaps. Mirrors the dsh profile-side configuration.
auto-install-peers=false
